# 📚 Документация по проверке соцсетей

## 🎯 Обзор

Модуль `social_checker` проверяет наличие телефона в социальных сетях и мессенджерах:
- **VK (ВКонтакте)** - через VK API
- **Telegram** - через сторонние провайдеры
- **WhatsApp** - через сторонние провайдеры
- **TikTok** - через сторонние провайдеры
- **Viber** - через сторонние провайдеры

## 🔧 Поддерживаемые провайдеры

### 1. VK API (Рекомендуется для начала)

**Преимущества:**
- ✅ Бесплатно
- ✅ Официальный API
- ✅ Не требует дополнительных подписок

**Ограничения:**
- ⚠️ Может не найти приватные профили
- ⚠️ Работает только для публичных номеров
- ⚠️ Требуется Service Token (не Ads API token)

**Настройка:**

1. **Получение Service Token:**
   - Перейдите на https://dev.vk.com/
   - Создайте приложение (тип: Standalone)
   - Получите Service Token:
     - Метод 1: Через OAuth (Authorization Code Grant)
     - Метод 2: Используйте существующий токен от VK Ads интеграции (если есть)
   
2. **Добавьте в `.env`:**
   ```env
   VK_API_TOKEN=your_vk_service_token_here
   ```

3. **Документация VK API:**
   - https://dev.vk.com/ru/method/users.search
   - https://dev.vk.com/ru/api/access-token/authcode-flow-user

**Пример использования:**
```python
from lead_validator.services.social_checker import social_checker

result = await social_checker.check_phone("+79991234567")
if result.has_vk:
    print(f"Найден VK профиль: {result.vk_profile_url}")
```

---

### 2. GetContact API (Платный, более точный)

**Преимущества:**
- ✅ Высокая точность проверки
- ✅ Поддержка Telegram, WhatsApp, Viber
- ✅ Дополнительные данные (username)

**Недостатки:**
- ❌ Платный (~$0.01 за проверку)
- ❌ Требует регистрации и пополнения баланса

**Настройка:**

1. **Регистрация:**
   - Перейдите на https://getcontact.com/
   - Зарегистрируйтесь и получите API ключ
   - Пополните баланс

2. **Добавьте в `.env`:**
   ```env
   GETCONTACT_API_KEY=your_getcontact_api_key
   ```

**Примечание:** Реальный endpoint API может отличаться. Уточните документацию на сайте GetContact.

---

### 3. NumBuster API (Платный, альтернатива)

**Преимущества:**
- ✅ Альтернативный провайдер
- ✅ Поддержка Telegram, WhatsApp, TikTok
- ✅ Хорошая точность

**Недостатки:**
- ❌ Платный
- ❌ Требует регистрации

**Настройка:**

1. **Регистрация:**
   - Перейдите на https://numbuster.com/
   - Зарегистрируйтесь и получите API ключ

2. **Добавьте в `.env`:**
   ```env
   NUMBUSTER_API_KEY=your_numbuster_api_key
   ```

**Примечание:** Реальный endpoint API может отличаться. Уточните документацию на сайте NumBuster.

---

## 🚀 Использование

### В коде валидации

Проверка соцсетей автоматически выполняется при валидации лида, если:
1. Включена в настройках проекта: `enable_social_check = true`
2. Настроен хотя бы один провайдер (VK API, GetContact или NumBuster)

```python
# В validators.py уже интегрировано:
if project and project.enable_social_check:
    social_result = await social_checker.check_phone(lead.phone)
    # Результаты сохраняются в lead_record
```

### Ручная проверка

```python
from lead_validator.services.social_checker import social_checker

# Проверка телефона
result = await social_checker.check_phone("+79991234567")

# Проверка конкретной соцсети
has_telegram = await social_checker.check_telegram("+79991234567")
has_whatsapp = await social_checker.check_whatsapp("+79991234567")
has_vk = await social_checker.check_vk("+79991234567")
```

---

## 📊 Структура результата

```python
@dataclass
class SocialCheckResult:
    phone: str                          # Номер телефона
    has_telegram: Optional[bool]        # Есть ли Telegram
    has_whatsapp: Optional[bool]        # Есть ли WhatsApp
    has_tiktok: Optional[bool]         # Есть ли TikTok
    has_vk: Optional[bool]             # Есть ли VK
    has_viber: Optional[bool]          # Есть ли Viber
    
    # Дополнительные данные
    telegram_username: Optional[str]    # Username в Telegram
    vk_profile_url: Optional[str]       # URL профиля VK
    vk_user_id: Optional[int]          # ID пользователя VK
    tiktok_username: Optional[str]      # Username в TikTok
    
    # Статус
    checked: bool                       # Была ли выполнена проверка
    error: Optional[str]                # Ошибка если была
    provider: Optional[str]             # Какой провайдер использовался
```

---

## ⚡ Кеширование

Результаты проверки кешируются в Redis на 7 дней для оптимизации:
- Избегаем повторных запросов к API
- Экономим деньги на платных провайдерах
- Ускоряем валидацию

Кеш автоматически используется при повторных проверках того же номера.

---

## 🔄 Приоритет провайдеров

Проверка выполняется в следующем порядке:

1. **VK API** (если настроен)
   - Быстрая проверка VK профилей
   - Если найден результат, остальные провайдеры не вызываются

2. **GetContact** (если настроен и VK не дал результатов)
   - Более точная проверка мессенджеров
   - Если найден результат, NumBuster не вызывается

3. **NumBuster** (если настроен и предыдущие не дали результатов)
   - Альтернативный провайдер
   - Проверка всех доступных соцсетей

---

## 📝 Пример конфигурации

### Минимальная конфигурация (VK API)

```env
# VK API для проверки соцсетей
VK_API_TOKEN=your_vk_service_token
```

### Полная конфигурация

```env
# VK API (бесплатно)
VK_API_TOKEN=your_vk_service_token

# GetContact API (платно)
GETCONTACT_API_KEY=your_getcontact_api_key

# NumBuster API (платно)
NUMBUSTER_API_KEY=your_numbuster_api_key
```

---

## 🧪 Тестирование

### Через API

```bash
# Тестовая валидация с проверкой соцсетей
curl -X POST "http://localhost:8000/api/lead/test-validate" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79991234567",
    "email": "test@example.com",
    "name": "Test User"
  }'
```

### Через код

```python
from lead_validator.services.social_checker import social_checker

# Тест проверки
result = await social_checker.check_phone("+79991234567")
print(f"VK: {result.has_vk}")
print(f"Telegram: {result.has_telegram}")
print(f"WhatsApp: {result.has_whatsapp}")
print(f"Provider: {result.provider}")
```

---

## ⚠️ Важные замечания

1. **VK API Token:**
   - Это НЕ тот же токен, что для VK Ads API
   - Нужен Service Token или User Token с правами на `users.search`
   - Можно получить через OAuth или использовать существующий токен

2. **Приватность:**
   - VK API может не найти приватные профили
   - Платные провайдеры обычно более точные

3. **Стоимость:**
   - VK API - бесплатно
   - GetContact/NumBuster - платно (~$0.01 за проверку)
   - Кеширование помогает экономить на повторных проверках

4. **Fail-open режим:**
   - Если все провайдеры недоступны, проверка пропускается
   - Лид не отклоняется из-за недоступности проверки соцсетей

---

## 🔗 Полезные ссылки

- [VK API Documentation](https://dev.vk.com/ru/method/users.search)
- [VK API Access Token](https://dev.vk.com/ru/api/access-token/authcode-flow-user)
- [GetContact API](https://getcontact.com/api)
- [NumBuster API](https://numbuster.com/api)

---

## 📈 Рекомендации

1. **Начните с VK API** - бесплатно и достаточно для базовой проверки
2. **Добавьте платный провайдер** если нужна более точная проверка мессенджеров
3. **Используйте кеширование** - экономит деньги и ускоряет проверку
4. **Мониторьте лимиты** - особенно для платных провайдеров

