# Тарифы и докупка мест — 16 сентября 2026

## Статус

Выпущено на production 16.09.2026, feature commit `52bd8c9`, прайс-бук версии 2. Новые покупки и смены тарифа — по новому каталогу, действующим подпискам сохраняются зафиксированные цены. Массового изменения будущих списаний и оплаченных периодов нет.

## Матрица

| Тариф | Месяц | Год | Проекты | Кабинеты | Пользователи | Запросы ассистента | Доп. проект/месяц |
|---|---:|---:|---:|---:|---:|---:|---:|
| Старт | 2 900 ₽ | 29 000 ₽ | 3 | 9 | 2 | 50 | 1 100 ₽ |
| Агентство | 6 900 ₽ | 69 000 ₽ | 10 | 30 | 6 | 250 | 800 ₽ |
| Про | 13 900 ₽ | 139 000 ₽ | 25 | 75 | 15 | 700 | 650 ₽ |

На лендинге и странице тарифов добавлены отдельные строки кабинетов, AI-комментариев/детектора без ограничений, каналов, экспорта/расписания и докупки. Исходный дизайн карточек сохранён; высота лендинговых карточек адаптирована под полный список. White Label не менялся и остаётся по заявке.

Существующее серверное правило годовой докупки: скидка 17%, годовые цены места 10 956 / 7 968 / 6 474 ₽. Изменения схемы скидок не вносились. Надпись из макета про зачёт остатка при повышении тарифа не перенесена: действующий checkout берёт полную стоимость и начинает новый период, интерфейс продолжает честно сообщать об этом.

## Исправления покупки

- Адаптивный баннер и модальное окно, ограничение ширины/высоты, внутренний скролл на низком экране, переносы, Escape, ограничение фокуса, возврат фокуса, тёмная тема.
- До покупки запрашивается серверная котировка: сумма сейчас, число доступных мест после покупки, полная сумма следующего продления, месяц/год.
- Ошибка расчёта не позволяет начать оплату; есть повтор запроса.
- В режиме исчерпанного запаса кнопка покупки раньше игнорировалась вызывающим кодом. Теперь обрабатывается так же, как в режиме подтверждения.
- Покупается нужное число мест: текущее количество + новый проект − оплаченный лимит. Пример: 4 из 3 → 2 места, не 1.
- Защита от параллельного запуска платёжных окон. Проект создаётся только после успешного виджета и подтверждения количества мест сервером; при задержке webhook выводится сообщение, без создания.
- Повторное открытие модального окна завершает предыдущий ожидающий запрос, не оставляет зависшие обещания.
- Баннер обновляется после подтверждённой покупки и при переходах.
- Сервер блокирует докупку при истёкшем/отсутствующем оплаченном периоде и при запланированной смене тарифа/периода/мест, чтобы покупка не перезаписала рекуррентную сумму будущего изменения.
- Пропорциональный платёж ограничен стоимостью полного периода.
- Незавершённый заказ, резервирующий последние места, не выдаётся за необходимость повысить тариф: отдельно объясняется ожидание оплаты/освобождения резерва.
- Триал видит актуальный каталог и оплачивает его цену даже при покупке того же тарифа; платёжный intent сохраняет новый snapshot. У оплаченной подписки сохранена логика зафиксированной цены.
- Отображение следующего списания учитывает дополнительные места, их запланированное уменьшение и отключённое автопродление.

## Проверки

- 46 серверных тестов: test_billing_slot_purchase.py, test_billing_guards.py, test_economics_master.py.
- Запуск на действующем backend image в отдельном контейнере: network none, SQLite, read-only, без production env и БД. CloudPayments не вызывается.
- 11 Node-тестов: node --test scripts/test-project-slots.mjs (из каталога frontend).
- Production build успешен на изолированной копии production-исходников с точечными изменениями. Чужие правки MainLayout/SignIn/старого landing исключены.
- Изолированный браузер с mock API: тарифы/модалка 1440/768/390/320 px, высота 500 px, сценарий ошибки котировки, отсутствие горизонтального overflow; лендинг 1920/1440/768/390/320 px, отсутствие наложений списка/кнопок/White Label.
- Реального списания и живого webhook в рамках этой задачи не было. Это необходимо явно отличать от проверки логики тестами.

Сохраняется существующее правило: неоплаченный intent держит резерв до 15 минут. Закрытие платёжного окна не снимает резерв немедленно, поскольку callback и webhook могут запаздывать. Интерфейс теперь объясняет это.

## Выпуск после решения владельца

1. Сверить текущие runtime images/env и сохранённый release snapshot. Нельзя делать blanket pull/build: production checkout cdf0a4d, поверх него точечный релиз вложений cb434c3. В HEAD есть другие невыкаченные DevOps-изменения.
2. Если старые цены сохраняются: до замены env зафиксировать отсутствующие snapshots действующих оплаченных подписок из старого прайс-бука; проверить существующие snapshots и рекуррентные суммы. Автоплатежи старых подписок не менять.
3. Если обновляются и старые продления: отдельно согласовать перечень подписок/даты, подготовить изменения snapshots и рекуррентных сумм CloudPayments с аудитом и откатом. Простая смена env не обновляет уже созданные рекурренты.
4. Конфигурация новых цен: ops/billing/prices.env.example. Версия 2. Существующий BILLING_PRICE_BOOK_JSON проверить и объединить, не затереть иные настройки. Обновить все процессы, использующие прайс-бук; не перезапускать automation без сверки её текущего образа и задач.
5. Точечно выкатить billing.py, frontend bundle и public/landing-new/index.html + assets/css/main.css. Обе landing-копии изменены, public/landing-new — production источник. CSS имеет новый cache-buster.
6. Проверить каталог/checkout новых покупок, цену старой подписки, котировки месяца/года, блокировки/White Label. Реальная контрольная оплата — только с разрешения владельца.

## Production: результат и откат

### Последующее обновление фронтенда: плашка слотов

**Актуальное состояние после корректировки размеров 16.09.2026:** выпущены `a165bff` + `9e82576`. Плашка использует масштаб страницы (`rem`), а кнопки слотов и привязки карты — общие правила высоты, шрифта, радиуса и отступов. На сенсорных экранах обе группы имеют минимум 44 px. На 320 px текст кнопки помещается в одну строку.

- Активный frontend: `sha256:1b36b676b9cadca75befb5d03c66ac4c1177b0a7099e652af46360163eba4767`, тег `admirra-frontend:slots-9e82576`.
- **Использовать для следующего выпуска:** `/root/admirra-frontend-slots-9e82576/active.json`.
- Откат этой корректировки, только после проверки отсутствия последующих релизов: `docker compose -p admirra --project-directory /root/Admirra -f /root/admirra-frontend-slots-9e82576/original.json up -d --no-deps --no-build --pull never frontend`.
- Повторно прошли сборка, 11 Node-тестов и UI-проверки 320–1440 px, включая равенство высоты/шрифта кнопок слотов и привязки карты, сенсорный режим, отсутствие переполнения и отмену подтверждения покупки. Публичные HTML/JS/CSS совпали со сборкой. Backend/automation не перезапускались, `.env` и лендинг неизменны.

История первоначального выпуска плашки:

16.09.2026 выкачен `151f57a`: адаптивная плашка «Докупленные слоты» на странице тарифов. Боковые отступы, отдельные количество/стоимость, перенос кнопок, зона нажатия от 44 px, тёмная тема. Платёжные обработчики не менялись.

- Frontend image первого выпуска: `sha256:8276b10f43abaf4dd388a32303573106aec7fa65cdfffb9794a9a52eeb9cba69`, тег `admirra-frontend:slots-151f57a`.
- Standalone Compose-конфигурация первого выпуска: `/root/admirra-frontend-slots-151f57a/active.json`. Предыдущая сохранена рядом в `original.json`; скрипт выпуска и `result.json` также в этом каталоге.
- Образ построен поверх frontend `billing-52bd8c9`, заменены только `dist/assets` и `dist/index.html`. Checkout сервера не обновлялся.
- Публичные `/settings`, `/ai`, основной JS/CSS и Settings JS/CSS побайтово совпали со сборкой. Главный лендинг не изменился; ID контейнеров backend/automation и `.env` не изменились.
- До выпуска: успешная сборка, 11 Node-тестов, изолированная UI-проверка 1440/768/390/320 px, нулевого количества и длинных чисел, обеих тем, открытия котировки на 1 слот и отмены подтверждений без покупки.
- Откат **только этой UI-правки**, после проверки отсутствия последующих релизов: `docker compose -p admirra --project-directory /root/Admirra -f /root/admirra-frontend-slots-151f57a/original.json up -d --no-deps --no-build --pull never frontend`.

Указанный ниже общий откат тарифного релиза имеет защиту от drift и теперь остановится из-за нового frontend image. Не обходить эту защиту: сначала отдельно оценить/откатить последующие изменения.

### Исходный тарифный релиз 52bd8c9

- Backend: `sha256:047c8019bbbeec83c0c8cd11b39c03b31af2931d8e2f0b415196199f8768afe0`.
- Frontend: `sha256:5a04a23235f01978a720f80d3bd937fc8ad9732ed46d230ed95e5c1ae3670823`.
- Automation: прежний образ `sha256:33b4ca03408c55cd11093fa3c4f9b86ecf1f323f50636222a43ea71bb4217ab8`, обновлены только переменные цен. Планировщик подтвердил 03:00 МСК синхронизации / 05:00 МСК отчёты.
- Активный release bundle: `/root/admirra-billing-release-52bd8c9`.
- Root-only snapshot: `/root/admirra-billing-backups/20260916T071847Z`; `active.json` — активная standalone Compose-конфигурация с точными образами/env.
- У 15 из 30 подписок отсутствовал snapshot: он заполнен старыми условиями до смены env. Сигнатуры цен всех 30 подписок совпали до/после, включая будущие запланированные тарифы. Вызовов CloudPayments не было.
- Backend и automation получили одну версию нового прайс-бука. Остальные env сохранены побайтово по значениям; nginx, почта, AI-шлюзы, DB, admin_frontend не изменялись.
- Первая попытка активации безопасно остановилась из-за двух running SyncJob до любых изменений. Выпуск выполнен после их завершения, при running_sync_jobs=0 и sending_reports=0.
- Финальные 46 тестов прошли на собранном новом backend image без сети и production-БД.
- `verify_release.py` проверил точное совпадение публичного лендинга, CSS, HTML /ai и /settings, JS bundle и billing.py с релизом. HTTP-проверки успешны.

Откат:

```sh
python3 /root/admirra-billing-release-52bd8c9/ops/billing/deploy.py --rollback /root/admirra-billing-backups/20260916T071847Z
```

Проверяет отсутствие последующих изменений env/nginx/images. Возвращает прежние образы и env. Снимки подписок не удаляет: они сохраняют цены, в том числе для покупок, сделанных после релиза.

**Важно:** checkout сервера остаётся `cdf0a4d`. Не запускать обычный `docker compose up --build` из него: он заменит точечный релиз старым кодом. Следующий деплой должен опираться на указанные активные образы/active.json или на отдельно согласованную production-ветку.
