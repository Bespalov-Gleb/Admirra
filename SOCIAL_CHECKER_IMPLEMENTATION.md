# ✅ Реализация проверки соцсетей - Завершено

## 📋 Что было сделано

### 1. Реализован модуль `social_checker.py`

**Файл:** `trafic_agent/lead_validator/services/social_checker.py`

**Функционал:**
- ✅ Проверка VK через VK API (`users.search`)
- ✅ Поддержка GetContact API (платный провайдер)
- ✅ Поддержка NumBuster API (платный провайдер)
- ✅ Кеширование результатов в Redis (TTL 7 дней)
- ✅ Нормализация номеров телефонов
- ✅ Обработка ошибок и fail-open режим
- ✅ Приоритетная проверка провайдеров

**Поддерживаемые соцсети:**
- VK (ВКонтакте)
- Telegram
- WhatsApp
- TikTok
- Viber

### 2. Обновлена конфигурация

**Файл:** `trafic_agent/lead_validator/config.py`

**Добавлены настройки:**
- `VK_API_TOKEN` - токен для VK API
- `GETCONTACT_API_KEY` - ключ для GetContact API
- `NUMBUSTER_API_KEY` - ключ для NumBuster API

### 3. Добавлено кеширование в Redis

**Файл:** `trafic_agent/lead_validator/services/redis_service.py`

**Новые методы:**
- `_get_cached_result()` - получение из кеша
- `_cache_result()` - сохранение в кеш

**Преимущества:**
- Экономия денег на платных провайдерах
- Ускорение повторных проверок
- TTL 7 дней

### 4. Обновлена документация

**Созданные файлы:**
- `SOCIAL_CHECKER_DOCS.md` - подробная документация
- `PHONE_SETUP_CHECKLIST.md` - обновлен с информацией о проверке соцсетей

---

## 🚀 Как использовать

### Минимальная настройка (VK API)

1. **Получите VK API Token:**
   - Перейдите на https://dev.vk.com/
   - Создайте приложение (Standalone)
   - Получите Service Token или используйте существующий от VK Ads

2. **Добавьте в `.env`:**
   ```env
   VK_API_TOKEN=your_vk_service_token
   ```

3. **Включите в настройках проекта:**
   - При создании/редактировании PhoneProject
   - Установите `enable_social_check = true`

4. **Готово!** Проверка будет выполняться автоматически при валидации лидов.

### Полная настройка (с платными провайдерами)

1. **VK API** (как выше)

2. **GetContact API:**
   ```env
   GETCONTACT_API_KEY=your_getcontact_api_key
   ```

3. **NumBuster API:**
   ```env
   NUMBUSTER_API_KEY=your_numbuster_api_key
   ```

---

## 📊 Структура данных

### SocialCheckResult

```python
@dataclass
class SocialCheckResult:
    phone: str                          # Номер телефона
    has_telegram: Optional[bool]        # Есть ли Telegram
    has_whatsapp: Optional[bool]        # Есть ли WhatsApp
    has_tiktok: Optional[bool]         # Есть ли TikTok
    has_vk: Optional[bool]             # Есть ли VK
    has_viber: Optional[bool]          # Есть ли Viber
    
    # Дополнительные данные
    telegram_username: Optional[str]
    vk_profile_url: Optional[str]
    vk_user_id: Optional[int]
    tiktok_username: Optional[str]
    
    # Статус
    checked: bool
    error: Optional[str]
    provider: Optional[str]  # "VK API", "GetContact", "NumBuster"
```

### Сохранение в Lead

Результаты сохраняются в модель `Lead`:
- `has_telegram`, `has_whatsapp`, `has_tiktok`, `has_vk` - булевы поля
- `social_accounts_data` - JSON с дополнительными данными (username, profile_url)

---

## 🔄 Приоритет провайдеров

1. **VK API** (если настроен)
   - Проверяет VK профили
   - Если найден результат, остальные провайдеры не вызываются

2. **GetContact** (если настроен и VK не дал результатов)
   - Проверяет мессенджеры (Telegram, WhatsApp, Viber)
   - Более точный, но платный

3. **NumBuster** (если настроен и предыдущие не дали результатов)
   - Альтернативный провайдер
   - Проверяет все доступные соцсети

---

## ⚡ Оптимизация

### Кеширование

- Результаты кешируются в Redis на 7 дней
- Ключ кеша: `social_check:{normalized_phone}`
- Автоматически используется при повторных проверках

### Fail-open режим

- Если все провайдеры недоступны, проверка пропускается
- Лид не отклоняется из-за недоступности проверки соцсетей
- Ошибки логируются, но не блокируют валидацию

---

## 🧪 Тестирование

### Через валидацию лида

```bash
curl -X POST "http://localhost:8000/api/lead/test-validate" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79991234567",
    "email": "test@example.com",
    "name": "Test User"
  }'
```

### Через Python код

```python
from lead_validator.services.social_checker import social_checker

result = await social_checker.check_phone("+79991234567")
print(f"VK: {result.has_vk}")
print(f"Telegram: {result.has_telegram}")
print(f"Provider: {result.provider}")
```

---

## 📝 Примеры использования

### Проверка конкретной соцсети

```python
# Проверка только VK
has_vk = await social_checker.check_vk("+79991234567")

# Проверка только Telegram
has_telegram = await social_checker.check_telegram("+79991234567")

# Проверка только WhatsApp
has_whatsapp = await social_checker.check_whatsapp("+79991234567")
```

### Полная проверка

```python
result = await social_checker.check_phone("+79991234567")

if result.checked:
    print(f"Провайдер: {result.provider}")
    if result.has_vk:
        print(f"VK профиль: {result.vk_profile_url}")
    if result.has_telegram:
        print(f"Telegram: {result.telegram_username}")
else:
    print(f"Ошибка: {result.error}")
```

---

## ⚠️ Важные замечания

1. **VK API Token:**
   - Это НЕ тот же токен, что для VK Ads API
   - Нужен Service Token или User Token
   - Можно использовать существующий токен от VK Ads интеграции

2. **Приватность профилей:**
   - VK API может не найти приватные профили
   - Платные провайдеры обычно более точные

3. **Стоимость:**
   - VK API - бесплатно
   - GetContact/NumBuster - платно (~$0.01 за проверку)
   - Кеширование экономит деньги

4. **Лимиты API:**
   - VK API: ~3 запроса/сек (зависит от токена)
   - GetContact/NumBuster: зависит от тарифа

---

## 🔗 Ссылки на документацию

- [VK API users.search](https://dev.vk.com/ru/method/users.search)
- [VK API Access Token](https://dev.vk.com/ru/api/access-token/authcode-flow-user)
- [GetContact API](https://getcontact.com/api)
- [NumBuster API](https://numbuster.com/api)

---

## ✅ Статус реализации

- [x] Реализован модуль `social_checker.py`
- [x] Добавлена поддержка VK API
- [x] Добавлена поддержка GetContact API
- [x] Добавлена поддержка NumBuster API
- [x] Реализовано кеширование в Redis
- [x] Обновлена конфигурация
- [x] Интегрировано в валидацию лидов
- [x] Создана документация
- [x] Обновлен чеклист

**Готово к использованию!** 🎉

