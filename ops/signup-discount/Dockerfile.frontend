ARG BASE_IMAGE
FROM ${BASE_IMAGE}
ARG APP_RELEASE
LABEL ru.admirra.signup-discount.revision=${APP_RELEASE}
# Keep the deployed landing and public files; retain old chunks for open tabs.
COPY dist/assets/ /usr/share/nginx/html/assets/
COPY dist/index.html /usr/share/nginx/html/index.html
