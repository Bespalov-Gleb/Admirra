# 📋 Чеклист для полноценной работы блока телефонии

## ✅ Что уже реализовано

### Backend
- ✅ Модель `PhoneProject` в базе данных
- ✅ API для управления проектами (`/api/phone-projects/`)
- ✅ Модуль `lead_validator` с валидацией лидов
- ✅ Webhook роутеры для Tilda, Marquiz, кастомных форм
- ✅ Сервисы валидации (DaData, Redis, Telegram, Метрика, CAPTCHA, UTM)

### Frontend
- ✅ Компонент `PhoneProjects.vue` для управления проектами
- ✅ Компонент `PhoneAPI.vue` для тестирования валидации
- ✅ Роуты `/phone-projects` и `/phone-api`

---

## 🔧 Что нужно настроить

### 1. **Переменные окружения (.env)**

Добавьте следующие переменные в `.env` файл:

```env
# ============================================================================
# DaData API (обязательно для валидации телефонов)
# ============================================================================
DADATA_API_KEY=your_dadata_api_key
DADATA_SECRET_KEY=your_dadata_secret_key
DADATA_TIMEOUT=5.0

# ============================================================================
# Redis (для rate limiting и дедупликации)
# ============================================================================
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=true

# ============================================================================
# Telegram Bot (для уведомлений о новых лидах)
# ============================================================================
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
TELEGRAM_ENABLED=true

# ============================================================================
# Яндекс.Метрика (для отправки офлайн-конверсий)
# ============================================================================
METRICA_COUNTER_ID=your_metrica_counter_id
METRICA_OAUTH_TOKEN=your_metrica_oauth_token
METRICA_ENABLED=true

# ============================================================================
# Yandex SmartCaptcha (для защиты от ботов)
# ============================================================================
SMARTCAPTCHA_CLIENT_KEY=your_smartcaptcha_client_key
SMARTCAPTCHA_SERVER_KEY=your_smartcaptcha_server_key
SMARTCAPTCHA_ENABLED=true

# ============================================================================
# Настройки валидации (опционально)
# ============================================================================
# Антибот настройки
MIN_FORM_FILL_TIME_SEC=3
MAX_FORM_FILL_TIME_SEC=3600

# Rate Limiting
RATE_LIMIT_PER_IP=10
RATE_LIMIT_WINDOW_SEC=3600

# Дедупликация
PHONE_DUPLICATE_TTL_SEC=86400

# Fail-open режим (пропускать при недоступности внешних сервисов)
FAIL_OPEN_MODE=true

# ============================================================================
# Проверка соцсетей (Social Checker)
# ============================================================================
# VK API (бесплатно, но ограниченно - только публичные профили)
VK_API_TOKEN=your_vk_service_token

# GetContact API (платно, ~$0.01 за проверку, более точный)
GETCONTACT_API_KEY=your_getcontact_api_key

# NumBuster API (платно, альтернативный провайдер)
NUMBUSTER_API_KEY=your_numbuster_api_key
```

---

## 📦 Инфраструктура

### 2. **Redis**

Redis необходим для:
- Дедупликации телефонов (защита от повторных заявок)
- Rate limiting по IP (защита от спама)

**Установка:**
```bash
# Docker
docker run -d -p 6379:6379 redis:latest

# Или через docker-compose.yml
redis:
  image: redis:latest
  ports:
    - "6379:6379"
```

**Проверка:**
```bash
redis-cli ping
# Должен вернуть: PONG
```

---

## 🔑 API ключи и токены

### 3. **DaData API** (обязательно)

**Назначение:** Валидация и нормализация телефонов, проверка email

**Регистрация:**
1. Перейдите на https://dadata.ru/
2. Зарегистрируйтесь и создайте API ключ
3. Получите `API Key` и `Secret Key`
4. Добавьте в `.env`

**Тарифы:** От 1000₽/месяц (1000 запросов)

---

### 4. **Telegram Bot** (рекомендуется)

**Назначение:** Уведомления о новых валидных лидах

**Создание бота:**
1. Напишите @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Получите токен бота
4. Узнайте Chat ID:
   - Напишите боту любое сообщение
   - Откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `chat.id` в ответе
5. Добавьте в `.env`

---

### 5. **Яндекс.Метрика** (рекомендуется)

**Назначение:** Отправка офлайн-конверсий для отслеживания качества лидов

**Настройка:**
1. Создайте OAuth токен в Яндекс ID:
   - https://oauth.yandex.ru/
   - Права: `metrika:read`, `metrika:write`
2. Получите Counter ID из настроек Метрики
3. Добавьте в `.env`

**Альтернатива:** Можно использовать существующий токен от Яндекс.Директ интеграции

---

### 6. **Yandex SmartCaptcha** (рекомендуется)

**Назначение:** Защита форм от ботов

**Настройка:**
1. Перейдите в Yandex Cloud: https://cloud.yandex.ru/
2. Создайте капчу в сервисе SmartCaptcha
3. Получите `Client Key` (для frontend) и `Server Key` (для backend)
4. Добавьте в `.env`

**Примечание:** Можно использовать бесплатный тариф для тестирования

---

## 🚀 Дополнительные функции

### 7. **Проверка соцсетей** (SocialChecker) ✅ РЕАЛИЗОВАНО

**Текущий статус:** ✅ Реализовано с поддержкой нескольких провайдеров

**Поддерживаемые провайдеры:**

#### VK API (бесплатно, но ограниченно)
- Использует VK API `users.search` для поиска профилей
- Требуется Service Token (не Ads API token)
- Может не найти приватные профили
- **Настройка:**
  ```env
  VK_API_TOKEN=your_vk_service_token
  ```
- **Получение токена:** https://dev.vk.com/ru/api/access-token/authcode-flow-user

#### GetContact API (платно)
- Более точная проверка Telegram, WhatsApp, Viber
- Платный тариф (~$0.01 за проверку)
- **Настройка:**
  ```env
  GETCONTACT_API_KEY=your_getcontact_api_key
  ```
- **Регистрация:** https://getcontact.com/api

#### NumBuster API (платно)
- Альтернативный провайдер для проверки соцсетей
- Поддерживает Telegram, WhatsApp, TikTok
- **Настройка:**
  ```env
  NUMBUSTER_API_KEY=your_numbuster_api_key
  ```
- **Регистрация:** https://numbuster.com/api

**Для активации:**
1. Выберите провайдера (рекомендуется начать с VK API - бесплатно)
2. Получите API ключи
3. Добавьте в `.env`
4. Включите в настройках проекта: `enable_social_check = true`

**Получение VK API Token:**
1. Перейдите на https://dev.vk.com/
2. Создайте приложение (тип: Standalone)
3. Получите Service Token:
   - Вариант 1: Используйте существующий токен от VK Ads интеграции (если есть)
   - Вариант 2: Получите через OAuth (Authorization Code Grant)
   - Документация: https://dev.vk.com/ru/api/access-token/authcode-flow-user
4. Добавьте в `.env`: `VK_API_TOKEN=your_token`

**Приоритет проверки:**
1. VK API (если настроен) - проверяет VK профили
2. GetContact (если настроен и VK не дал результатов) - проверяет мессенджеры
3. NumBuster (если настроен и предыдущие не дали результатов) - альтернатива

**Кеширование:**
- Результаты проверки кешируются в Redis на 7 дней
- Экономит деньги на платных провайдерах
- Ускоряет повторные проверки

**Подробная документация:** См. `SOCIAL_CHECKER_DOCS.md`

---

### 8. **Проверка Госуслуг** (Gosuslugi Check)

**Текущий статус:** Не реализовано

**Для реализации:**
1. Изучите API Госуслуг (если доступно)
2. Или используйте сторонние сервисы
3. Реализуйте в `lead_validator/services/gosuslugi_checker.py`
4. Включите в настройках проекта: `enable_gosuslugi_check = true`

---

## 📊 Проверка работоспособности

### Тестирование компонентов

1. **DaData:**
   ```bash
   curl -X GET "http://localhost:8000/api/lead/check-phone?phone=+79991234567" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

2. **Telegram:**
   ```bash
   curl -X GET "http://localhost:8000/api/lead/test-telegram" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

3. **Метрика:**
   ```bash
   curl -X GET "http://localhost:8000/api/lead/test-metrica" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

4. **Полная валидация (тест):**
   ```bash
   curl -X POST "http://localhost:8000/api/lead/test-validate" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{
       "phone": "+79991234567",
       "email": "test@example.com",
       "name": "Test User"
     }'
   ```

5. **Проверка соцсетей (через валидацию):**
   - Проверка соцсетей выполняется автоматически при валидации лида
   - Убедитесь, что в настройках проекта включено: `enable_social_check = true`
   - Результаты сохраняются в поле `social_accounts_data` лида

---

## 🎯 Минимальная конфигурация для работы

Для базовой работы телефонии достаточно:

1. ✅ **DaData API** (обязательно)
2. ✅ **Redis** (рекомендуется, но можно отключить)
3. ⚠️ **Telegram** (опционально, для уведомлений)
4. ⚠️ **Метрика** (опционально, для конверсий)
5. ⚠️ **SmartCaptcha** (опционально, для защиты)

**Без Redis:**
- Дедупликация не будет работать
- Rate limiting не будет работать
- Но валидация через DaData будет работать

**Без Telegram/Метрики:**
- Валидация будет работать
- Но уведомления и конверсии не будут отправляться

---

## 📝 Следующие шаги

1. **Настройте переменные окружения** в `.env`
2. **Запустите Redis** (если используете)
3. **Получите API ключи** для DaData (обязательно)
4. **Создайте Telegram бота** (рекомендуется)
5. **Настройте Яндекс.Метрику** (рекомендуется)
6. **Протестируйте через PhoneAPI** компонент
7. **Создайте первый проект** через `/phone-projects`
8. **Настройте webhook** в Tilda/Marquiz

---

## 🔍 Проверка готовности

После настройки проверьте:

- [ ] Redis запущен и доступен
- [ ] DaData API ключи работают (тест через `/api/lead/check-phone`)
- [ ] Telegram бот отвечает (тест через `/api/lead/test-telegram`)
- [ ] Метрика подключена (тест через `/api/lead/test-metrica`)
- [ ] SmartCaptcha настроена (если используется)
- [ ] Проверка соцсетей настроена (VK_API_TOKEN или другой провайдер)
- [ ] Создан хотя бы один PhoneProject
- [ ] Webhook URL проекта доступен извне
- [ ] Тестовая валидация работает (`/api/lead/test-validate`)
- [ ] Проверка соцсетей работает (включена в настройках проекта)

---

## 💡 Рекомендации

1. **Начните с минимальной конфигурации:**
   - DaData + Redis
   - Остальное добавляйте по мере необходимости

2. **Мониторинг:**
   - Следите за лимитами DaData API
   - Проверяйте логи Redis
   - Отслеживайте уведомления в Telegram

3. **Безопасность:**
   - Не храните API ключи в коде
   - Используйте переменные окружения
   - Настройте rate limiting для защиты от спама

4. **Масштабирование:**
   - Redis можно вынести в отдельный сервис
   - DaData имеет лимиты по тарифу
   - Telegram имеет rate limits (30 сообщений/сек)

---

## 📚 Дополнительная документация

- [DaData API](https://dadata.ru/api/clean/phone/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Яндекс.Метрика API](https://yandex.ru/dev/metrika/)
- [Yandex SmartCaptcha](https://cloud.yandex.ru/docs/smartcaptcha/)
- [Redis Documentation](https://redis.io/docs/)

