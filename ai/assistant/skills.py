"""Скиллы и инструкции ассистента: плейбук под AdMirra + справочник Yandex Metrika.

PROJECT_PLAYBOOK всегда идёт в системный промпт (компактно). METRIKA_REFERENCE
объёмный — отдаётся отдельным инструментом `metrika_reference`, чтобы агент тянул
его только когда строит запрос к Метрике (гигиена контекста).

Справочник Метрики частично основан на open-source skill yandex-metrika
(artwist-polyakov/polyakov-claude-skills) и офиц. документации Reporting API v1.
"""
from __future__ import annotations

PROJECT_PLAYBOOK = """\
# Как работать (AdMirra)

Ты — аналитик рекламы AdMirra. У пользователя есть проекты; у проекта подключены
рекламные площадки (Яндекс.Директ, Яндекс.Метрика, VK Реклама, Avito Реклама) —
у разных проектов разный набор. Твоя задача — отвечать по РЕАЛЬНЫМ данным через
инструменты, кратко и по делу, с конкретными числами.

## Порядок действий
1. Определи проект. Если он ещё не выбран в диалоге — вызови `list_projects`,
   найди по названию и вызови `use_project`. После этого смотри поле `platforms`
   (yandex/vk/avito) — используй только инструменты подключённых площадок.
2. Определи период. Если пользователь не назвал — бери разумный (обычно текущий
   или прошлый месяц) и явно скажи, за какой период считал. Формат дат YYYY-MM-DD.
3. Возьми данные нужными инструментами, посчитай, ответь.

## Что учитывать
- **Цели/конверсии:** считай по ОТСЛЕЖИВАЕМЫМ целям проекта — это те, что
  пользователь отметил на дашборде (в ответе `use_project` — поле `tracked_goals`,
  или вызови `metrika_get_tracked_goals`). Не бери все цели счётчика подряд.
  Конверсия по цели в Метрике — метрика `ym:s:goal<ID>reaches`.
- **CPL/CPA:** стоимость лида = расход ÷ число целевых конверсий за период.
- **Площадки:** для расхода/кликов по Директу — `direct_get_statistics`; по VK —
  `vk_get_statistics`; по Avito — `avito_get_statistics`. Метрика — про сайт/цели.
- **НДС:** расходы Директа приходят с НДС по умолчанию.
- **Wordstat** — общий спрос в Яндексе по фразам; проект не нужен. Вызывай только
  когда спрашивают про спрос/семантику/частотность.

## Стиль и эффективность
- Не переусложняй: на простой вопрос («баланс VK», «расход за июль») — один-два
  вызова и краткий ответ. Не делай лишних и повторяющихся вызовов.
- Если чего-то нет (площадка не подключена, нет данных) — честно скажи, не выдумывай.
- Отвечай структурно: короткий вывод сверху, затем цифры/разбивка при необходимости.
"""


METRIKA_REFERENCE = """\
# Справочник Yandex Metrika Reporting API (для metrika_get_report / by_time)

Эндпоинты (через инструменты): `/stat/v1/data` (таблица, metrika_get_report),
`/stat/v1/data/bytime` (динамика, metrika_get_report_by_time).

## Метрики визитов (префикс ym:s:)
- ym:s:visits — визиты; ym:s:users — уникальные посетители; ym:s:pageviews — просмотры;
- ym:s:bounceRate — отказы; ym:s:pageDepth — глубина; ym:s:avgVisitDurationSeconds — ср. длительность.

## Метрики целей (подставить <goal_id>)
- ym:s:goal<goal_id>reaches — достижения цели (это и есть «конверсии/лиды»);
- ym:s:goal<goal_id>visits — визиты с достижением; ym:s:goal<goal_id>users — пользователи;
- ym:s:goal<goal_id>conversionRate — конверсия (по визитам).

## Измерения источников (подставить атрибуцию <attr> = lastsign|last|first; дефолт lastsign)
- ym:s:<attr>TrafficSource — тип источника; ym:s:<attr>SourceEngine — детальный источник;
- ym:s:<attr>AdvEngine — рекламная система; ym:s:<attr>ReferalSource — реферал.

## UTM-измерения (подставить <attr>)
- ym:s:<attr>UTMSource / UTMMedium / UTMCampaign / UTMContent / UTMTerm.

## Прочие измерения
- ym:s:date — дата; ym:s:deviceCategory — desktop/mobile/tablet; ym:s:regionCountry / regionCity;
- ym:s:lastDirectClickOrder — кампания Директа (связка с рекламой).

## Фильтры (параметр filters)
Примеры: `ym:s:isRobot=='No'`, `ym:s:deviceCategory=='mobile'`,
`ym:s:lastsignTrafficSource=='organic'`. Операторы: == != =@ (содержит) !@ =~ (regex);
объединять AND / OR. По умолчанию полезно отсекать роботов: ym:s:isRobot=='No'.

## Важные ограничения (проверено)
- НЕЛЬЗЯ смешивать в одном запросе префиксы ym:s: и ym:pv: (визиты и просмотры — разные scope).
- ym:ad:* (расходы Директа в Метрике) — отдельный scope, требует direct_client_logins; не мешать с ym:s:.
- bytime (динамика) возвращает максимум ~7 значений измерения как колонки, остальные молча отбрасываются —
  для дробных разрезов лучше делать отдельные запросы `metrika_get_report` по периодам.
- accuracy=1 — без сэмплирования (точные данные), если инструмент это поддерживает.

## Рецепты
- Конверсии по отслеживаемой цели: metrics=`ym:s:goal<ID>reaches`, dimensions=`ym:s:date` (для динамики — by_time).
- Источники трафика: dimensions=`ym:s:lastsignTrafficSource`, metrics=`ym:s:visits,ym:s:users`.
- Разбивка по кампаниям Директа из Метрики: dimensions=`ym:s:lastDirectClickOrder`.
"""
